function binary(bin){

  
    // // binary to decimal
    // ConvertBase.bin2dec = function (num) {
    //     return ConvertBase(num).from(2).to(10);
    // };

   console.log(output);
  
}

binary(00001001)